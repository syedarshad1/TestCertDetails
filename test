SELECT
    API_ID,
    API_UUID,
    API_PROVIDER,
    API_NAME,
    API_VERSION,
    CONTEXT,
    ORGANIZATION,
    STATUS,
    UPDATED_BY,
    TO_CHAR(UPDATED_TIME, 'YYYY-MM-DD HH24:MI:SS') AS UPDATED_TIME
FROM   COCT42_AM.AM_API
ORDER BY UPDATED_TIME DESC;  -- today first, oldest last
